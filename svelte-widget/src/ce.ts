import CgmTir from './lib/CgmTir.svelte'

// When built with customElement: true, Svelte will register the tag automatically
// if the component is compiled as a custom element. Here we just export so Vite bundles it.
export default CgmTir

